alias tg topgrade
;; ls
; alias sl ls
; alias l ls -lAh $*
; alias ll ls -l
; alias la ls -a
; alias lla ls -la

alias l exa -lah --icons
alias ldot exa -ld .*
alias sl ls
;; alias ls exa
alias la exa -a
alias lla exa -la --icons
alias ll exa -lh --git --icons

;; docker
alias dk docker $1
alias dkc docker-compose $1

;; eshell
;; alias ff find-file $1
alias ff find-file-other-window $1
